apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-todo-delete-tasks-api-service"
spec:
  type: ClusterIP
  selector:
    app: "{{ .Release.Name }}-todo-delete-tasks-api"
  ports:
  - port: {{ .Values.delete-task.servicePort }}
    targetPort: {{ .Values.delete-task.port }}
