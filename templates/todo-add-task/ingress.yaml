apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todo-add-task-api-ingress
spec:
  ingressClassName: azure-application-gateway
  rules:
    - host: add-tasks-api.applicationtesting.online
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: todo-add-tasks-api-service
                port:
                  number: 80
